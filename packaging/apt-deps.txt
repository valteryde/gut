# Single source of truth for the desktop stack's system dependencies.
# Consumed by:
#   - backend/Dockerfile          (gut-desktop image)
#   - packaging/build-deb.sh      (gut-bot .deb Depends: line)
# Browser and postgres are handled per-format: the image picks google-chrome
# (amd64) or chromium (arm64); the deb always uses chromium + postgresql.
xvfb
x11vnc
websockify
novnc
xfce4
xfce4-terminal
dbus-x11
libreoffice-writer
libreoffice-calc
libreoffice-impress
scrot
xdotool
x11-utils
xclip
wmctrl
python3
python3-venv
python3-pip
python3-tk
fonts-liberation
fonts-dejavu-core
fonts-noto-color-emoji
xdg-utils
curl
wget
ca-certificates
openssl
gnupg
sudo
procps
# Structured-control stack: the agent drives native apps through the AT-SPI
# accessibility tree (atspi.py needs the bus + python3-pyatspi) and the live
# LibreOffice document through python3-uno. libreoffice-gtk3 is required —
# LO only bridges to AT-SPI through the gtk3 VCL plugin (no-install-recommends
# would skip it). dconf-cli supplies the gsettings backend used to force
# toolkit-accessibility on.
at-spi2-core
python3-pyatspi
python3-uno
libreoffice-gtk3
dconf-cli
libglib2.0-bin
